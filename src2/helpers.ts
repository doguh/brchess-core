// import { PieceListState, PieceState } from './types';

// export function forEachPiece(
//   list: PieceListState,
//   callback: { (piece: PieceState, key: string, list: PieceListState): void }
// ): void {
//   Object.keys(list).forEach(
//     (key: string, index: number): void => callback(list[key], key, list)
//   );
// }
